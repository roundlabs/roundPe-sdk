// import { ENV, BASE_URL } from '../constants/constant';

// export const getHostUrl = (env: string) => {
//   if (env === ENV.PROD) {
//     return BASE_URL.PROD;
//   } else if (env === ENV.STAGE) {
//     return BASE_URL.STAGE;
//   } else if (env === ENV.DEV) {
//     return BASE_URL.DEV;
//   }
// };
